# WeChat 代码块 fence 退化模式

WeChat 文章的 HTML→Markdown 转换中，fenced code block 的 ``` 标记经常退化为单 `` ` `` 嵌入首行代码。

## 退化模式速查表

| 模式 | 原文特征 | 修复手法 | 示例 |
|------|---------|---------|------|
| Rust 代码 | 行首 `` `fn `` / `` `pub `` / `` `struct `` / `` `enum `` / `` `enum `` / `` `static `` / `` `let `` | 行首反引号 → ```rust\n，尾部补 ```\n | `` `fn inject(`` → ```rust\nfn inject(``` |
| YAML/配置 | 行首 `` `# `` / `` `  ``（缩进配置） | 行首反引号 → ```yaml\n | `` `# 只写引用`` → ```yaml\n# 只写引用``` |
| 内联代码合并 | 行首 `` `mfs_search.py`` 等（内联代码位于行首） | **保留**，非退化模式 | 合法内联代码 |
| 中文学术块 | 行首 `` `你提一个需求``，后续行以 │/▼ 制图符排列 | 行首 `` ` `` → ```\n，尾部 `` ` `` → \n``` | |
| Bash 命令 | 行首 `` `curl https://...`` / `` `npx ...`` / `` `claude ...`` | 行首反引号 → ```bash\n | |
| 对话/思考流 | 全文末行 `` `因此…``（反引号后接中文长句） | 行首反引号 → ```\n | |
| 行尾粘连 | 段落末尾 `` ` `` 后跟完整句子 | 反引号后插 \n\n 分离 | ``...干完了`这一圈`` → ``...干完了```\n\n这一圈 |
| 连续反引号合并 | `` `line1``line2``line3` ``（两行代码被合并为一行，双反引号间隔） | 按 `` `` 拆分后组装为 ``` 代码块 | |
| YAML Front Matter 连续合并 | `` `---``name: X``colors:``  primary: "#xxx"``---`` `` 等，整个 YAML 文档（含 `---` 起始符、缩进的 key-value）被 inline backtick 包裹合并成一行 | 提取整块文本，按 `` `` 拆分为独立行，重建为 ```yaml 多行 fenced block。注意恢复缩进层级（2/4 空格），移除 key 外侧多余引号 | `---``name: Heritage``colors:``  primary: "#1A1C1E"``  → ```yaml\n---\nname: Heritage\ncolors:\n  primary: "#1A1C1E"\n...\n``` |
| 行内 URL 单反引号 | 行首 `` `地址：https://...` `` 或 `` `开源地址：https://...` `` 后紧接正文 | 不是代码块退化，是 URL 被 wrap 成 inline code。删除行首反引号，保留 URL 作为超链接文本，在 URL 行后插入 `\n\n` 分离后续正文 | `` `地址：https://github.com/xxx`和 Awesome Design.md`` → `地址：https://github.com/xxx\n\n和 Awesome Design.md` |
| 代码块结尾粘连 | 闭合 ``` 后紧跟正文无换行，如 `}```四条命令分别干这几件事：` 或 `` ````如果你的项目已经用了 Tailwind`` | 在 ``` 后插入 `\n\n` 分离正文。检测：扫描 `\n```\n` 后是否紧接非空行且无换行 | `}```四条命令分别干这几件事：` → `}\n```\n\n四条命令分别干这几件事：` |

## 检测方法

1. 先扫描所有行首为 `` ` `` 的行（排除 ``` 标记行）
2. 根据反引号后第一个字符判断：
   - `fn` / `pub` / `struct` / `enum` / `static` / `let` → Rust 代码块
   - `# ` → YAML 或 shell 注释（需根据上下文判断）
   - `curl` / `npx` / `claude` / `hermes` / `git` → bash 命令
   - 中文/中文标点 → ASCII 流程图或对话
   - 代码标识符（字母/数字/下划线开头且有配对反引号）→ 内联代码，跳过
3. 对于连续反引号合并模式（`line1``line2`），按 `` `` 分割后每段为单独行，整体围入 ```...``` 

## 注意事项

- **不要全局替换**：全局替换行首 `` ` `` 会破坏行首合法的内联代码（如 `` `mfs_search.py` 和 `mfs_cat.py` ``）
- **尾部粘连**：代码块的最后一行之后往往紧接正文无换行（`}`挂完之后），必须分段处理
- **Rust 缩进**：Rust 代码的缩进可能是 4 空格，替换 fence 后不要清空缩进
- **代码块内中文注释**：Rust 代码中可以有中文注释（`// ① 工具定义`），不影响 fence 判定
- **代码块间有正文**：两个代码块之间隔有正文时，各自需要独立的 ``` 围栏，不能合并
